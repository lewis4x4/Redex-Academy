/**
 * F1 authoring-admin stub shell. Content-as-data authoring (sim specs, lessons,
 * fixtures, rubric line-items) is built in M9. Lovable is out of scope — the
 * admin is built in-repo.
 */
export default function App() {
  return (
    <main className="min-h-screen bg-slate-50 text-slate-900">
      <div className="mx-auto flex max-w-2xl flex-col gap-3 px-6 py-16">
        <h1 className="text-2xl font-bold text-redex">Redex Academy — Authoring</h1>
        <p className="text-slate-600">
          Authoring admin shell (F1). Content-as-data authoring arrives in M9.
        </p>
      </div>
    </main>
  );
}
