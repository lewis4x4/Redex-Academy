{
  "name": "@redex/auth",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared JWT custom-claim schema + parser (org_id/roles/persona/evaluator_authorized/domains) minted by academy.custom_access_token_hook (F3). Claims are a trust boundary — always Zod-parsed; persona drives UI, roles drive permissions.",
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
