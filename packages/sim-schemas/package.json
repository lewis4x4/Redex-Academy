{
  "name": "@redex/sim-schemas",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Published sim-engine spec contracts (F5): the shared envelope, per-engine JSON Schemas, and their Zod runtime mirrors + validateSpec. Mirror of sim-contracts; engine_kind is 1:1 with academy.sim_kind.",
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
