// The D1 component gallery — the single reviewable artifact for the human-verify
// gate. Renders every @redex/ui primitive in every state on the dark + Redex-red
// theme, with a persona-density toggle. The orchestrator wires <Gallery/> into a
// route; this barrel is its public entry.
export { default as Gallery } from './Gallery';
