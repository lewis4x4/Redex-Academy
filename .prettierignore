# Build output & deps
**/node_modules/**
**/dist/**
**/build/**
**/.turbo/**
**/coverage/**
**/playwright-report/**
**/test-results/**
pnpm-lock.yaml

# Generated, committed artifacts — never hand-edited
**/database.types.ts
**/*.gen.ts

# =============================================================================
# Authored build-kit sources — kept VERBATIM (do not reformat). These predate
# the monorepo scaffold and are governance/spec/seed content owned elsewhere.
# Later goals (F2/F5/S1) wire the SQL/JSON specs into supabase/ and packages/,
# where the monorepo's formatting WILL apply.
# =============================================================================
AGENT_ROLES_AND_ACCEPTANCE.md
CLAUDE.md
CODING_STANDARDS.md
DECISIONS_LEDGER.md
PRE_BUILD_CHECKLIST.md
README.md
SAFETY_GLOSSARY.md
adr/**
goals/**
migrations/**
seed/**
sim-contracts/**
supabase/migrations/**
supabase/tests/**

# Deno Edge Functions — separate runtime/toolchain (deno fmt / deno check), not prettier.
supabase/functions/**

# Issuer static identity docs — hand-authored published artifacts served verbatim
# by Netlify (the DID document + OB 3.0 issuer Profile). Their exact bytes are the
# did:web resolution target + the signed-over identity; do NOT let prettier reflow
# them (the `issuer` file is intentionally extensionless, mirroring the served URL).
apps/web/public/.well-known/**
